<div class="well new-user">
  <center>
  <%= link_to "Logout", session_path, :method => :delete %>
  <h1>Edit</h1>
  </center>
  <br/>
  <%= render 'form' %>
  <center>
  <% if @user.texting %>
    <%= button_to "Disable Texts", {action: "disable", remote: true}, {class: 'btn btn-danger'} %>
  <% else %>
    <%= button_to "Enable Texts", {action: "disable", remote: true}, {class: 'btn btn-warning'} %>
  <% end %>
  </center>
</div>